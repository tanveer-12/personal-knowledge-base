You are a senior Next.js + React engineer.

### Task
Implement V2 semantic notes UI.

### Features
- Note cards:
  - title, summary, tags, last updated, relevance score
- Search UI:
  - text input → calls `/notes/search`
  - displays results
- Sidebar:
  - recent notes, smart collections, tags
- Quick capture modal:
  - floating button, cmd+k shortcut
- Daily recall section:
  - shows older notes

### Constraints
- Do not rewrite unrelated components
- Use responsive Tailwind grid layout
- Only integrate with tested backend endpoints
- Provide example local test instructions (JSON mocks if needed)